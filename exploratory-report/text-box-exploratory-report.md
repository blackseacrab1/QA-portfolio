# Отчёт об исследовательском тестировании  
Объект тестирования: Text Box (раздел Elements) на [demoqa.com](https://demoqa.com/text-box)  
Дата тестирования: 15 ноября 2025 г.  
Продолжительность сессии: 30 минут  
Цель: выявить неочевидные поведения, риски и ограничения формы без заранее подготовленных тест-кейсов  

---

## Подход
Использован сессионный исследовательский тестинг:
- Не было предварительно написанных сценариев
- Фокус на граничные значения, нестандартный ввод, UX и валидацию
- Проверка как позитивных, так и негативных сценариев в реальном времени

---

## Проверенные сценарии

| Категория | Сценарий |
|----------|--------|
| Длина ввода | Ввод 500+ символов в поле Full Name и Current Address |
| Специальные символы | Использование !@#$%^&*()_+, emoji (например, 🧪), кириллица, арабские цифры |
| Email-валидация | Ввод: user, user@, @domain.com, user@domain, user@domain. |
| Пустой ввод | Отправка формы без заполнения полей |
| Пробелы | Ввод пробелов в начале и конце email ( user@example.com ) |
| Копирование/вставка | Вставка длинного текста из буфера обмена |
| Отправка без фокуса | Нажатие Submit без клика по полям (только через Tab или мышь) |

---

## Наблюдения и результаты

✅ Положительные:
- Форма корректно принимает кириллицу, emoji и спецсимволы  
- Нет ограничений на длину ввода — даже 1000+ символов обрабатываются без ошибок  
- Данные сохраняются и отображаются мгновенно под формой  
- Интерфейс не ломается при любом вводе

⚠️ Риски и замечания:
- Отсутствует клиентская валидация email:  
  Даже строка вроде abc или user@ принимается как валидный email  
- Пробелы в email не обрезаются:  
   user@example.com  сохраняется «как есть», что может вызвать проблемы при дальнейшей обработке
- Нет подсказок о формате email — пользователь не знает, что ожидается валидный адрес

---

## Выводы
Форма Text Box устойчива к нестандартному вводу, но не обеспечивает базовую валидацию email. Это может привести к некорректным данным в системе, особенно если бэкенд ожидает валидный email.

## Рекомендации
1. Добавить клиентскую валидацию email по шаблону: /.+@.+\..+/  
2. Автоматически обрезать пробелы по краям полей (trim())  
3. Добавить placeholder: example@domain.com для поля Email

> Отчёт подготовлен в рамках портфолио QA-инженера.  
> Автор: [blackseacrab1](https://github.com/blackseacrab1)
